<h1>Update Project</h1>

<%= render partial: 'form', locals: { project: @project } %>

<p>
  <%= link_to 'Back to Projects', projects_path, class: 'text-decoration-none' %>
  |
  <%= link_to 'Back to Project', project_path(@project), class: 'text-decoration-none' %>
</p>
